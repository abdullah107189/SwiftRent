import React from "react";

const SectionHeader = ({ title, subTitle }) => {
  return (
    // to do working title
    <div className="">
      <h2>{subTitle}</h2>
      <h1>{title}</h1>
    </div>
  );
};

export default SectionHeader;
